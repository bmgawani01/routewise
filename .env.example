# Local development: copy to .env. On a host (Render/Aiven) set these in the dashboard instead.
NODE_ENV=development
PORT=5000

DB_HOST=localhost
DB_PORT=3306
DB_USER=routewise_app
DB_PASSWORD=change-me
DB_NAME=routewise
DB_SSL=false

ALLOWED_ORIGIN=http://localhost:5000
